# Helix Cheat Sheet

To generate the cheat sheet in your prefered theme
- copy your theme file to the `themes` directory and
- compile with `typst compile main.typ --input theme=themes/<theme>.toml`.
